import { Color } from "csstype";

export interface IChartColor {
    gray: Color;
}

export enum ChartColor {
    AXIS = "#444444",
    DOTS = "#444444",
    NET = "#aaaaaa",
    POLY = "tomato",
    DASH = "#888888",

}
